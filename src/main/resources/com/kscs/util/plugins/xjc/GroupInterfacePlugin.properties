usage.usage=Usage
usage=Transforms <group> and <attributeGroup> XSD definitions to java interface declarations, and makes complexTypes using \
  the group implement this interface.
usage.options=Options
usage.opt.declareSetters=Also generate property setter methods in interface declarations.
usage.opt.declareBuilderInterface=Also generate fluent-builder interfaces in interface declarations.